rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can read/write to their own draft application data.
    match /users/{userId}/{path=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Rules for the top-level applications collection.
    match /applications/{appId} {
      // Allow users to create their own application.
      allow create: if request.auth.uid == request.resource.data.userId;

      // Allow users to read their own application, and admins to read any.
      allow read: if (request.auth != null && request.auth.uid == resource.data.userId) || (request.auth.token.admin == true);

      // Only allow admins to update or delete submitted applications.
      allow update, delete: if request.auth.token.admin == true;
    }

    // Admins can read/write settings and news.
    match /settings/{docId} {
      allow read, write: if request.auth.token.admin == true;
    }
    match /news/{docId} {
       allow read, write: if request.auth.token.admin == true;
    }
  }
}
